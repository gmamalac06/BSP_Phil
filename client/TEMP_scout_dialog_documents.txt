// ADD THIS CODE TO scout-form-dialog.tsx at line 332 (after the unit Select, before the status field)

            {/* Documents */}
            <div className="space-y-4">
              <h3 className="font-semibold text-sm">Documents</h3>
              <div className="grid grid-cols-2 gap-4">
                <div className="space-y-2">
                  <Label htmlFor="paymentProof">Payment Proof</Label>
                  <Input
                    id="paymentProof"
                    type="file"
                    accept="image/*,.pdf"
                    onChange={(e) => {
                      const file = e.target.files?.[0];
                      if (file) {
                        // TODO: Upload to Supabase storage
                        console.log("Payment proof file:", file.name);
                      }
                    }}
                  />
                  {formData.paymentProof && (
                    <p className="text-xs text-muted-foreground">
                      Current: <a href={formData.paymentProof} target="_blank" rel="noopener noreferrer" className="text-primary hover:underline">View file</a>
                    </p>
                  )}
                </div>
                <div className="space-y-2">
                  <Label htmlFor="profilePhoto">Profile Photo</Label>
                  <Input
                    id="profilePhoto"
                    type="file"
                    accept="image/*"
                    onChange={(e) => {
                      const file = e.target.files?.[0];
                      if (file) {
                        // TODO: Upload to Supabase storage
                        console.log("Profile photo:", file.name);
                      }
                    }}
                  />
                  {formData.profilePhoto && (
                    <p className="text-xs text-muted-foreground">
                      Current: <a href={formData.profilePhoto} target="_blank" rel="noopener noreferrer" className="text-primary hover:underline">View photo</a>
                    </p>
                  )}
                </div>
              </div>
            </div>
